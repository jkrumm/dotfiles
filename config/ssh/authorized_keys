# Trusted SSH public keys, installed into ~/.ssh/authorized_keys by
# `make authorized-keys` (append-if-missing; never clobbers existing keys) —
# also run as part of `make remote-access`. Public keys only — safe to commit.
# Keys here are served by the 1Password SSH agent and install on EVERY
# machine, including the mini. The mini->iumac key is intentionally not in
# this file — it lives in its own file, config/ssh/authorized_keys.iumac,
# installed only on a present-human machine; see that file's header for why.
#
# jkrumm — personal 1Password SSH key (Mac mini + MacBook)
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMNsgZhHFzXUs7+T8jfw1gwBL3ECgsw0ClOMEZ8gykIo jkrumm
