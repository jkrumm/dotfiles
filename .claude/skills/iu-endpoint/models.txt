# IU unified-endpoint model roster — one id per line, `<provider>/<model>`.
#
# provider is "iu-anthropic" (native Anthropic transport, best Claude fidelity,
# NOT EU-residency-guaranteed) or "iu" (OpenAI-compatible transport, `/openai/v1`).
# The model id itself may contain further slashes (e.g. a Qwen HF-style path) —
# only the FIRST slash is the provider separator; validate.sh splits on it.
#
# Comments and blank lines are ignored. Edit this file directly to add/drop a
# model from the health-check set. This is the roster actually in use — `ca`/`cap`
# (modelpick), sideclaw workers, research-gateway and Hermes — not a catalog
# sample; every id was verified live against /openai/v1/models on 2026-09-07.
# Hermes's own model config lives in ~/SourceRoot/hermes-agent, not here.

iu-anthropic/claude-sonnet-5
iu-anthropic/claude-opus-5
iu-anthropic/claude-haiku-4-5
iu-anthropic/DeepSeek-V4-Flash

iu/glm-5.3-flash
iu/deepseek-v4.1-flash
iu/gpt-6-luna
iu/gpt-5.6-sol
iu/gpt-5.6-terra
iu/gpt-6-astra
iu/gemini-3.8-flash
iu/gemini-3.5-flash
iu/gemini-3.5-flash-lite
